import React from 'react';
import ReactDOM from 'react-dom/client';

// import your current project here
import App from './projects/16-markdown-preview/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
