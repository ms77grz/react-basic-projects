import React from 'react';
import ReactDOM from 'react-dom/client';

// import your current project here
import App from './projects/21-movie-db/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
